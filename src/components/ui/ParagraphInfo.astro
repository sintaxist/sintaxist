---
interface Props {
    text: any;
    className?: string;
}

const { text, className } = Astro.props;
---

<p
    set:html={text}
    class={`${className} font-secondary text-sm xs:text-base sm:text-md md:text-lg leading-[170%]`}
/>
