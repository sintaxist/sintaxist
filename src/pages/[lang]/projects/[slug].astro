---
import Layout from "@/layouts/Layout.astro";
import { Image } from "astro:assets";
import Button from "@/components/ui/Button.astro";
import Pill from "@/components/ui/Pill.astro";
import SectionTitle from "@/components/ui/SectionTitle.astro";
import { LANGUAGES } from "@/locales";
import type { AppLocale } from "@/locales";
import ParagraphInfo from "@/components/ui/ParagraphInfo.astro";
import BackButton from "@/components/BackButton.astro";
import IconFigma from "@/icons/IconFigma.astro";
import LinkDetailProject from "@/components/ui/LinkDetailProject.astro";
import ItemInfoBlog from "@/components/ui/ItemInfoBlog.astro";

export async function getStaticPaths() {
  const paths = [];

  for (const lang of Object.keys(LANGUAGES)) {
    const t = LANGUAGES[lang as AppLocale].translations;

    const projectInfo = {
      slug: "yoorco",
      title:
        "Impulsando el crecimiento digital a través del diseño — Yoorco Landing Page",
      description:
        "Una landing page estratégica para Yoorco: rediseñada para reflejar profesionalismo, claridad y crecimiento tecnológico, impulsando su modelo de suscripción digital.",
      imgSrc: "https://i.imgur.com/Z0EcRIQh.jpeg",
      textFigmaUrl: t.projectsPage.textFigmaUrl,
      figmaUrl:
        "https://www.figma.com/design/kHpZdtjO6qXXCnRRcXUy8d/Landing-Yoorco?node-id=0-1&t=HnKMEsZBkmCl1JuT-1",
      figmaEmbed:
        "https://embed.figma.com/design/kHpZdtjO6qXXCnRRcXUy8d/Landing-Yoorco?node-id=0-1&embed-host=share",
      categoryIds: ["ui-ux", "branding"],

      overview: `
        <strong>Yoorco</strong> es una empresa mexicana que ofrece desarrollo de software por suscripción, 
        operando como un “departamento de tecnología externo” para startups y empresas en crecimiento.<br/><br/>
        El objetivo de este proyecto fue reimaginar su presencia digital, creando una landing page moderna, estratégica 
        y alineada con su visión de crecimiento tecnológico.<br/><br/>
        El resultado: una interfaz clara, limpia y flexible que transmite confianza y comunica valor con precisión.
      `,

      timeframe: "1 Semana",
      role: "Diseñador UI / UX",
      tools: "Figma, FigJam, Photoshop, Illustrator",

      challenge: {
        title: "El problema",
        description: `
          <strong>Yoorco</strong> necesitaba una landing page capaz de comunicar su servicio con precisión y consistencia visual,
          fortaleciendo la percepción de marca y facilitando la comprensión del modelo de neogcio.
        `,
      },

      goal: {
        title: "La meta",
        description:
          "Transmitir profesionalismo, claridad y enfoque tecnológico, alineando el branding con su propuesta de valor, y creando un sistema visual escalable y adaptable a cualquier dispositivo.",
      },

      process: {
        title: "Mi proceso de diseño",
        childs: [
          {
            title: "Empathize & Research",
            description:
              "Se analizaron referentes de empresas SaaS, estudios digitales y marcas tecnológicas con modelos de suscripción identificando patrones visuales efectivos:",
            list: [
              "Jerarquía visual clara (beneficios sobre funciones).",
              "Color como diferenciador, no saturación.",
              "Uso estratégico del espacio negativo y tipografía moderna.",
            ],
          },
          {
            title: "Define & Ideate",
            description:
              "Se propuso una estructura narrativa en 5 secciones clave:",
            list: [
              "Hero — Mensaje de valor directo + CTA.",
              "Metas con Yoorco — Beneficios concretos.",
              "Casos de Éxito — Confianza y resultados.",
              "Clientes — Validación social.",
              "Equipo — Humanización de la marca.",
            ],
          },
          {
            title: "Design & Prototype",
            description:
              "El rediseño se centró en una paleta cromática refinada y un sistema UI modular:",
            list: [
              `<span class="inline-block shadow-lg mr-1 rounded-full bg-[#F77F08] w-6 h-6 circle-color"></span> #F77F08 — dinamismo, energía y acción.`,
              `<span class="inline-block shadow-lg mr-1 rounded-full bg-[#086FFD] w-6 h-6 circle-color"></span> #086FFD — tecnología y confianza.`,
              `<span class="inline-block shadow-lg mr-1 rounded-full bg-[#FEFEFE] w-6 h-6 circle-color"></span> #FEFEFE — claridad visual.`,
              `<span class="inline-block shadow-lg mr-1 rounded-full bg-[#0E4085] w-6 h-6 circle-color"></span> #0E4085 — solidez y estabilidad.`,
            ],
          },
        ],
      },

      results_title: "Results",
      results_description: `
        <p>El nuevo diseño permitió a Yoorco proyectar una identidad más profesional y confiable, con una comunicación clara del modelo de negocio.</p>
        <p>El enfoque en la jerarquía visual y el contenido estratégico generó una mejor comprensión del servicio y una experiencia más fluida para el usuario.</p>
        <h4>💡 Impacto clave:</h4>
        <ul>
          <li>+100% en claridad del mensaje visual.</li>
          <li>Identidad digital sólida y escalable.</li>
          <li>Mayor consistencia de marca entre medios digitales.</li>
          <li>Base visual lista para integración en Astro.</li>
        </ul>
      `,

      future_title: "Future Opportunities",
      future_description: `
        <p>El siguiente paso natural para Yoorco sería integrar métricas UX y analítica de interacción,
        permitiendo medir el rendimiento de los CTAs, tiempos de permanencia y puntos de abandono.</p>
        <p>Además, el sistema visual está preparado para escalar hacia un diseño de producto completo,
        incluyendo dashboards, paneles administrativos o micrositios especializados.</p>
      `,
    };

    for (const project of t.projectsPage.projects) {
      paths.push({
        params: { lang, slug: project.slug },
        props: {
          project: projectInfo,
          lang: lang as AppLocale,
          header: t.header,
          sections: t.sections,
          footer: t.footer,
          categories: t.projectsPage.categories,
          ctaBack: t.projectsPage.ctaBack,
        },
      });
    }
  }

  return paths;
}

const { project, lang, header, sections, footer, categories, ctaBack } =
  Astro.props;
---

<Layout
  title={`${project.title} — Caso de Estudio`}
  description={project.intro}
  headerInfo={header}
  sections={sections}
  footerInfo={footer}
>
  <article
    class="max-w-5xl mx-auto px-6 sm:px-8 flex flex-col gap-8 md:gap-16 pt-25 md:pt-30 animation-group text-primary font-secondary"
    transition:name={project.slug}
  >
    <header class="flex flex-col gap-4">
      <BackButton info={ctaBack} />
      <h1
        class="md:text-center font-primary text-2xl sm:text-3xl md:text-5xl text-primary mb-4 leading-[130%] anim-item"
        data-animate="text"
      >
        {project.title}
      </h1>
      <div class="flex flex-wrap gap-2 md:justify-center">
        {
          project.categoryIds?.map((cat, index) => (
            <Pill label={categories[cat] || cat} {index} primary delay={200} />
          ))
        }
      </div>
      <Image
        src={project.imgSrc}
        alt={`Diseño del proyecto ${project.title}`}
        width={1200}
        height={400}
        class="w-full m-auto shadow-lg rounded-lg mt-2 anim-item"
        data-animate="text"
        data-delay={200}
      />
      <div class="flex items-center gap-4 mt-2">
        {
          project.figmaUrl && (
            <LinkDetailProject
              url={project.figmaUrl}
              text={project.textFigmaUrl}
              icon="figma"
            />
          )
        }
      </div>
    </header>

    <section class="flex flex-col gap-6 md:gap-10">
      <SectionTitle
        color="primary"
        text="Overview"
        tag="h2"
        iconPosition="left"
      />
      <div class="flex gap-6 flex-col-reverse md:flex-row">
        <ul class="flex flex-col gap-6 md:min-w-[25%]">
          <li class="flex flex-col gap-3 flex-1">
            <h3 class="underline font-primary text-xl md:text-2xl">
              Tiempo de desarrollo:
            </h3>
            <p class="text-sm md:text-base lg:text-md">{project.timeframe}</p>
          </li>
          <li class="flex flex-col gap-3 flex-1">
            <h3 class="underline font-primary text-xl md:text-2xl">Mi rol:</h3>
            <p class="text-sm md:text-base lg:text-md">{project.role}</p>
          </li>
          <li class="flex flex-col gap-3 flex-1">
            <h3 class="underline font-primary text-xl md:text-2xl">Herramientas:</h3>
            <p class="text-sm md:text-base lg:text-md">{project.tools}</p>
          </li>
        </ul>
        <div class="flex flex-col gap-4">
          <h3 class="font-primary text-4xl mb-2 hidden md:block">Contexto</h3>
          <ParagraphInfo text={project.overview} />
        </div>
      </div>
    </section>

    <hr class="w-full text-primary" />

    <section class="flex flex-col gap-6 md:gap-10">
      <ItemInfoBlog
        title={project.challenge.title}
        description={project.challenge.description}
      />
      <ItemInfoBlog
        title={project.goal.title}
        description={project.goal.description}
      />
    </section>

    <hr class="w-full text-primary" />

    <section class="flex flex-col gap-8 md:gap-16">
      <SectionTitle
        color="primary"
        text={project.process.title}
        tag="h2"
        iconPosition="left"
      />
      <div class="flex flex-col gap-6 md:gap-10">
        {
          project.process.childs.map((child) => (
            <ItemInfoBlog
              title={child.title}
              description={child.description}
              list={child.list}
            />
          ))
        }
      </div>
    </section>

    <hr class="w-full text-primary" />

    <!-- <div>

      <section>
        <SectionTitle color="primary" text={project.results_title} tag="h2" />
        <div class="mt-4" set:html={project.results_description} />
      </section>

      <section>
        <SectionTitle color="primary" text={project.future_title} tag="h2" />
        <div class="mt-4" set:html={project.future_description} />
      </section>

      <section>
        <SectionTitle color="primary" text="📎 Prototype" tag="h2" />
        <p class="mt-2">Visualiza el diseño directamente desde Figma:</p>
        <iframe
          src={project.figmaEmbed}
          class="w-full h-[400px] md:h-[600px] mt-8 rounded-lg shadow-md border border-gray-200"
          allowfullscreen></iframe>
      </section>

      <Button
        as="a"
        href={`/${lang}/projects`}
        text={ctaBack}
        fill={false}
        color="primary"
      />
    </div> -->
  </article>
</Layout>
