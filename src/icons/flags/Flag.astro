---
import UsaFlag from '@/icons/flags/UsaFlag.astro';
import MexicoFlag from '@/icons/flags/MexicoFlag.astro';

const { code, class: className } = Astro.props;
---
{code === 'es' && <MexicoFlag class={className} />}
{code === 'en' && <UsaFlag class={className} />}