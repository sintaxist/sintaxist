---
import PackageCard from "@/components/PackageCard.astro";
import PackagesParticles from "@/components/PackagesParticles.astro";
import Button from "@/components/ui/Button.astro";
import ComparisonTable from "@/components/ComparisonTable.astro";
import * as Icons from "@/components/ui/Icons.ts";
import { packages } from "@/data/paquetes";
---

<section id="paquetes" class="py-20 lg:py-32 px-6 relative">
  <PackagesParticles />

  <div class="max-w-7xl mx-auto relative z-10">
    <div class="text-center mb-16 animate-on-scroll">
      <div
        class="inline-flex items-center space-x-2 bg-gradient-to-r from-emerald-100 to-blue-100 text-emerald-700 px-6 py-3 rounded-full border border-emerald-200 shadow-lg text-center"
      >
        <span class="text-lg">🎉</span>
        <span class="  font-semibold">
          Promoción de Lanzamiento: 20% de Descuento en tu Primer Evento
        </span>
      </div>
    </div>

    <div class="md:text-center mb-32">
      <h2 class="  font-bold text-4xl lg:text-6xl text-gray-900 mb-6">
        Paquetes de <span class="text-indigo-600">Invitaciones Digitales</span> para
        Cada Tipo de Evento
      </h2>
      <p
        class="  font-normal text-xl text-gray-600 max-w-3xl mx-auto leading-relaxed"
      >
        Desde celebraciones íntimas hasta experiencias de gran escala, cada
        paquete está diseñado para elevar tu evento con innovación, estilo y
        calidad excepcional.
      </p>
    </div>

    <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-8 lg:items-stretch">
      {packages.map((pkg, index) => <PackageCard pkg={pkg} index={index} />)}
    </div>

    <div class="mt-32">
      <h3 class="  font-bold text-3xl lg:text-4xl text-gray-900 mb-12 text-center animate-on-scroll">
        Comparativa de Paquetes
      </h3>
      <ComparisonTable />
    </div>

    <div class="text-center mt-20">
      <p class="  text-gray-600 mb-6">
        ¿Necesitas algo personalizado? Hablemos sobre tu proyecto específico.
      </p>
      <Button
        href="https://calendly.com/momenta-invitaciones/nueva-reunion"
        target="_blank"
        variant="floating"
        size="lg"
        icon={Icons.Sparkles}
      >
        Consulta Personalizada Gratuita
      </Button>
    </div>
  </div>
</section>
